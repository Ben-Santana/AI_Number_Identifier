# AI_Number_Identifier
Python program that uses my machine learning library to train and use a neural network trained on MNIST number data.

To install dependencies:
```
pip install -r requirements.txt
```

To run:
  ```
  python3 app.py
  ```
